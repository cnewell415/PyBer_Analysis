Step 1
    Visually Review Data Sets
Step 2
    Load CSV files
        os.path.join()
    Inspect
        -Look for Null Values
            df.count() OR df.isnull().sum()
        -Check datatypes
            df.dtypes
    Clean
Step 3
    Merge Data Sets
        new_df = pd.merge(leftdf, rightdf, on=["column_leftdf", "column_rightdf"], how="left")
Step 4 
    Split Data out into other DataFrames to be worked on
        groupby()
Step 5
    Create Data Series needed for the X-Axis and Y-Axis (plus more if needed, bubble size for example) 
Step 6
    Create your plots or charts using the Data Series
        -Add labels and colors
Step 7
    Calculate Summary Statistics
        1) .describe()
        2) mean() | median() | mode()
        3) np.mean() | np.median() | sts.mode()
Step 8 
    Show any Outliers by visualizing the Summary Statistics
        Box-n-Whisker Plots
Step 9 
    Create Alternative Charts to show the same info or enhance the information